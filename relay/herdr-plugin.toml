id = "herdr-mobile-relay.events"
name = "Herdr Mobile Relay Events"
version = "0.1.0"
min_herdr_version = "0.7.0"
description = "Push local Herdr agent events to Herdr Mobile Relay"
platforms = ["macos", "linux"]

[[events]]
on = "pane.agent_status_changed"
command = ["python3", "on_event.py"]
